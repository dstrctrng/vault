#!/bin/bash -e

pth_install=$1; shift

# cache software
rsync -ia vault/software/. $pth_install/software/ --delete

# cache downloads, isos
rsync -ia vault/virtualbox/. $pth_install/virtualbox/ --delete

# cache distfiles
rsync -ia vault/macports/. $pth_install/macports/ --delete
